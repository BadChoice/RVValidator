source 'https://github.com/CocoaPods/Specs.git'


platform :ios, '8.0'

target 'RVValidator' do

pod 'Collection'
end

target 'RVValidatorTests'  do
#Needs to be here but empty so it uses the parent pods without creating the 2 classes
end


